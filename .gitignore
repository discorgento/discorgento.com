# Dependencies
node_modules/

# Build output
dist/

# Astro generated types
.astro/

# Wrangler (Cloudflare)
# .wrangler/ may contain sensitive local state; a local wrangler.toml would
# override the deploy pipeline config — if one exists, delete it.
# Deploy is Cloudflare Pages (no wrangler config file needed).
.wrangler/
wrangler.toml

# Environment
.env
.env.production

# Misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*